type Movie {
    id: Int!
    name: String!
    director: String!
    score: Float!
}

type Query {
    movies: [Movie]!
    movie(id: Int!): Movie
}

type Mutation {
    addMovie(name: String!, director: String!, score: Float!): Movie!
}